    //  factorial calculation

let number = Number(prompt("enter the number"));
let product = 1;
for(let i=1 ; i <= number ; i++)
    {
      product = product * i ;
    } 
alert("sonuc: " + product);